--- a/parser.c
+++ b/parser.c
@@ -10795,6 +10795,9 @@
 	ctxt->inSubset = 1;
 	xmlParseDocTypeDecl(ctxt);
 	if (RAW == '[') {
+#ifdef MAGMA_ENABLE_CANARIES
+    MAGMA_LOG("MAGMA_RECORD_xmlParseDocument", RAW == '[' && NXT(1) == '<');
+#endif
 	    ctxt->instate = XML_PARSER_DTD;
 	    xmlParseInternalSubset(ctxt);
 	    if (ctxt->instate == XML_PARSER_EOF)
