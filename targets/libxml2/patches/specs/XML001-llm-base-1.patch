--- a/valid.c
+++ b/valid.c
@@ -821,6 +821,10 @@
     /* TODO: should we rebuild in this case ? */
     if (elem->contModel != NULL) {
 	if (!xmlRegexpIsDeterminist(elem->contModel)) {
+#ifdef MAGMA_ENABLE_CANARIES
+    MAGMA_LOG("MAGMA_RECORD_xmlValidBuildContentModel", elem->contModel != NULL && !xmlRegexpIsDeterminist(elem->contModel));
+#endif
+
 	    ctxt->valid = 0;
 	    return(0);
 	}
@@ -1298,6 +1302,12 @@
     int len;
 
     if (content == NULL) return;
+#ifdef MAGMA_ENABLE_CANARIES
+if (size - strlen(buf) < 0) {
+    MAGMA_LOG("MAGMA_RECORD_xmlSnprintfElementContent", 1);
+}
+#endif
+
     len = strlen(buf);
     if (size - len < 50) {
 	if ((size - len > 4) && (buf[len - 1] != '.'))
@@ -5355,6 +5365,9 @@
 	if (!xmlRegexpIsDeterminist(elemDecl->contModel)) {
 	    return(-1);
 	}
+#ifdef MAGMA_ENABLE_CANARIES
+    MAGMA_LOG("MAGMA_RECORD_xmlValidateElementContent", (warn) && ((ret != 1) && (ret != -3)));
+#endif
 	ctxt->nodeMax = 0;
 	ctxt->nodeNr = 0;
 	ctxt->nodeTab = NULL;
--- a/debugXML.c
+++ b/debugXML.c
@@ -584,6 +584,15 @@
 static void
 xmlCtxtDumpElemDecl(xmlDebugCtxtPtr ctxt, xmlElementPtr elem)
 {
+#ifdef MAGMA_ENABLE_CANARIES
+if (elem && elem->name && elem->content) {
+    size_t nameLen = xmlStrlen(elem->name);
+    size_t contentLen = elem->content ? xmlStrlen((char *)elem->content->name) : 0; // Assuming elem->content->name is an xmlChar*
+    if (nameLen + contentLen > 5000) { // Arbitrary threshold for demonstration
+        MAGMA_LOG("MAGMA_RECORD_xmlCtxtDumpElemDecl", 1);
+    }
+}
+#endif
     xmlCtxtDumpSpaces(ctxt);
 
     if (elem == NULL) {
